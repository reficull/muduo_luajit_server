module(..., package.seeall)


local cySession = cySession or {}

function cyDispatch(msg)
    --看是否有進行中
    --如果沒有開始遊戲InitGame
    --如果有，比對成語第一個字是否是當前成語最後一個字
    --如果是，查看成語是否存在
    --如果存在，返回成語
end

function InitGame()
    --獲得隨機成語
    --當前成語寫入cySession
    --返回成語
end



